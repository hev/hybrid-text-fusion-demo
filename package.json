{
  "name": "hybrid-text-fusion-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that serves the SciFact HybridText search UI and proxies search to the Layer gateway.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
